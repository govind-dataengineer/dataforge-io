# Local Development Environment Overrides
environment: dev

storage:
  endpoint: "http://localhost:9000"
  access_key: "minioadmin"
  secret_key: "minioadmin"

database:
  host: "localhost"
  port: 5432
  user: "dataforge"
  password: "dataforge_dev"
  database: "dataforge_db"

spark:
  master: "local[4]"  # Local mode for development

api:
  host: "0.0.0.0"
  port: 8000
